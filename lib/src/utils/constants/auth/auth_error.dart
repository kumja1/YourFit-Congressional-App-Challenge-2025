class AuthError {
  static const String googleSignInError =
      "An error occured when signing in with Google";

  static const String userNotFound = "User not found";

  static const String invalidOAuthProvider = "Invalid OAuth provider";
}
